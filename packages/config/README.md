# Config package

Этот пакет хранит общие настройки для инструментов разработки турборепозитория: ESLint, Prettier и Ruff.
Используйте эти файлы из приложений в `apps/*` и других пакетов, чтобы держать правила единообразными.
